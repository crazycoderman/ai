# Groq API Key
# Get one at https://console.groq.com/keys
GROQ_API_KEY=gsk_your_api_key_here

# NOTE: If deploying to Vercel, add this as an Environment Variable in your project settings.
# For local development with Vite, this might need to be prefixed VITE_ depending on configuration,
# but the app checks process.env.GROQ_API_KEY which works with standard process shims.